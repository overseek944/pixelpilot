{
  "_comment": "Copy into your project root and replace /ABSOLUTE/PATH/TO/pixelpilot with where you cloned this repo. Or just run: claude mcp add pixelpilot -- npx tsx /ABSOLUTE/PATH/TO/pixelpilot/src/mcp/server.ts  (after `npm run build`, prefer: node /ABSOLUTE/PATH/TO/pixelpilot/dist/mcp/server.js).",
  "mcpServers": {
    "pixelpilot": {
      "type": "stdio",
      "command": "npx",
      "args": ["tsx", "/ABSOLUTE/PATH/TO/pixelpilot/src/mcp/server.ts"]
    }
  }
}
